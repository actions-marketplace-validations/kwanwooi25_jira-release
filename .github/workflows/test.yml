name: Test

on:
  workflow_dispatch:
    inputs:
      version:
        type: string
        required: true
      issueKeys:
        type: string
        required: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          githubToken: ${{ secrets.GITHUB_TOKEN }}
          jiraHost: ${{ secrets.JIRA_HOST }}
          jiraEmail: ${{ secrets.JIRA_EMAIL }}
          jiraApiToken: ${{ secrets.JIRA_API_TOKEN }}
          jiraProjectKey: 'ACTION'
          jiraVersionPrefix: 'v'
          jiraReleaseVersion: ${{ inputs.version }}
          slackWebhookUrl: 'https://hooks.slack.com/workflows/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'
